# Makefile generated by imake - do not edit!
# $XConsortium: imake.c,v 1.65 91/07/25 17:50:17 rws Exp $
#
# The cpp used on this machine replaces all newlines and multiple tabs and
# spaces in a macro expansion with a single space.  Imake tries to compensate
# for this, but is not always successful.
#

# -------------------------------------------------------------------------
# Makefile generated from "Imake.tmpl" and <Imakefile>
# $XConsortium: Imake.tmpl,v 1.139 91/09/16 08:52:48 rws Exp $
#
# Platform-specific parameters may be set in the appropriate <vendor>.cf
# configuration files.  Site-specific parameters should be set in the file
# site.def.  Full rebuilds are recommended if any parameters are changed.
#
# If your C preprocessor does not define any unique symbols, you will need
# to set BOOTSTRAPCFLAGS when rebuilding imake (usually when doing
# "make World" the first time).
#

# -------------------------------------------------------------------------
# site-specific configuration parameters that need to come before
# the platform-specific parameters - edit site.def to change

# site:  $XConsortium: site.def,v 1.2 91/07/30 20:26:44 rws Exp $

# -------------------------------------------------------------------------
# platform-specific configuration parameters - edit sun.cf to change

# platform:  $XConsortium: sun.cf,v 1.72.1.1 92/03/18 13:13:37 rws Exp $

# operating system:  SunOS 4.1.1

# $XConsortium: sunLib.rules,v 1.7 91/12/20 11:19:47 rws Exp $

# -------------------------------------------------------------------------
# site-specific configuration parameters that go after
# the platform-specific parameters - edit site.def to change

# site:  $XConsortium: site.def,v 1.2 91/07/30 20:26:44 rws Exp $

            SHELL = /bin/sh

              TOP = ../../../.
      CURRENT_DIR = ./src/newimage/template

               AR = ar clq
  BOOTSTRAPCFLAGS =
               CC = cc
               AS = as

         COMPRESS = compress
              CPP = /lib/cpp $(STD_CPP_DEFINES)
    PREPROCESSCMD = cc -E $(STD_CPP_DEFINES)
          INSTALL = install
               LD = ld
             LINT = lint
      LINTLIBFLAG = -C
         LINTOPTS = -axz
               LN = ln -s
             MAKE = make
               MV = mv
               CP = cp

           RANLIB = ranlib
  RANLIBINSTFLAGS =

               RM = rm -f
            TROFF = psroff
         MSMACROS = -ms
              TBL = tbl
              EQN = eqn
     STD_INCLUDES =
  STD_CPP_DEFINES = -DNOSTDHDRS -DX_WCHAR -DX_LOCALE -DXML -D_WCHAR_T
      STD_DEFINES = -DNOSTDHDRS -DX_WCHAR -DX_LOCALE -DXML -D_WCHAR_T
 EXTRA_LOAD_FLAGS =
  EXTRA_LIBRARIES =
             TAGS = ctags

    SHAREDCODEDEF = -DSHAREDCODE
         SHLIBDEF = -DSUNSHLIB

    PROTO_DEFINES =

     INSTPGMFLAGS =

     INSTBINFLAGS = -m 0755
     INSTUIDFLAGS = -m 4755
     INSTLIBFLAGS = -m 0644
     INSTINCFLAGS = -m 0444
     INSTMANFLAGS = -m 0444
     INSTDATFLAGS = -m 0444
    INSTKMEMFLAGS = -g kmem -m 2755

      PROJECTROOT = /usr/local

     TOP_INCLUDES = -I$(INCROOT)

      CDEBUGFLAGS = -O
        CCOPTIONS = -pipe

      ALLINCLUDES = $(INCLUDES) $(EXTRA_INCLUDES) $(TOP_INCLUDES) $(STD_INCLUDES)
       ALLDEFINES = $(ALLINCLUDES) $(STD_DEFINES) $(EXTRA_DEFINES) $(PROTO_DEFINES) $(DEFINES)
           CFLAGS = $(CDEBUGFLAGS) $(CCOPTIONS) $(ALLDEFINES)
        LINTFLAGS = $(LINTOPTS) -DLINT $(ALLDEFINES)

           LDLIBS = $(SYS_LIBRARIES) $(EXTRA_LIBRARIES)

        LDOPTIONS = $(CDEBUGFLAGS) $(CCOPTIONS) $(LOCAL_LDFLAGS) -L$(USRLIBDIR)

   LDCOMBINEFLAGS = -X -r
      DEPENDFLAGS =

        MACROFILE = sun.cf
           RM_CMD = $(RM) *.CKP *.ln *.BAK *.bak *.o core errs ,* *~ *.a .emacs_* tags TAGS make.log MakeOut

    IMAKE_DEFINES =

         IRULESRC = $(CONFIGDIR)
        IMAKE_CMD = $(IMAKE) -DUseInstalled -I$(IRULESRC) $(IMAKE_DEFINES)

     ICONFIGFILES = $(IRULESRC)/Imake.tmpl $(IRULESRC)/Imake.rules \
			$(IRULESRC)/Project.tmpl $(IRULESRC)/site.def \
			$(IRULESRC)/$(MACROFILE) $(EXTRA_ICONFIGFILES)

# -------------------------------------------------------------------------
# X Window System Build Parameters
# $XConsortium: Project.tmpl,v 1.138.1.1 92/11/11 09:49:19 rws Exp $

# -------------------------------------------------------------------------
# X Window System make variables; this need to be coordinated with rules

          PATHSEP = /
        USRLIBDIR = /usr/local/lib
           BINDIR = /usr/local/bin
          INCROOT = /usr/local/include
     BUILDINCROOT = $(TOP)
      BUILDINCDIR = $(BUILDINCROOT)/X11
      BUILDINCTOP = ..
           INCDIR = $(INCROOT)/X11
           ADMDIR = /usr/adm
           LIBDIR = $(USRLIBDIR)/X11
        CONFIGDIR = $(LIBDIR)/config
       LINTLIBDIR = $(USRLIBDIR)/lint

          FONTDIR = $(LIBDIR)/fonts
         XINITDIR = $(LIBDIR)/xinit
           XDMDIR = $(LIBDIR)/xdm
           TWMDIR = $(LIBDIR)/twm
          MANPATH = /usr/local/man
    MANSOURCEPATH = $(MANPATH)/man
        MANSUFFIX = n
     LIBMANSUFFIX = 3
           MANDIR = $(MANSOURCEPATH)$(MANSUFFIX)
        LIBMANDIR = $(MANSOURCEPATH)$(LIBMANSUFFIX)
           NLSDIR = $(LIBDIR)/nls
        PEXAPIDIR = $(LIBDIR)/PEX
      XAPPLOADDIR = $(LIBDIR)/app-defaults
       FONTCFLAGS = -t

     INSTAPPFLAGS = $(INSTDATFLAGS)

            IMAKE = imake
           DEPEND = makedepend
              RGB = rgb

            FONTC = bdftopcf

        MKFONTDIR = mkfontdir

	MKDIRHIER = mkdirhier

        CONFIGSRC = $(TOP)/config
       DOCUTILSRC = $(TOP)/doc/util
        CLIENTSRC = $(TOP)/clients
          DEMOSRC = $(TOP)/demos
           LIBSRC = $(TOP)/lib
          FONTSRC = $(TOP)/fonts
       INCLUDESRC = $(TOP)/X11
        SERVERSRC = $(TOP)/server
          UTILSRC = $(TOP)/util
        SCRIPTSRC = $(UTILSRC)/scripts
       EXAMPLESRC = $(TOP)/examples
       CONTRIBSRC = $(TOP)/../contrib
           DOCSRC = $(TOP)/doc
           RGBSRC = $(TOP)/rgb
        DEPENDSRC = $(UTILSRC)/makedepend
         IMAKESRC = $(CONFIGSRC)
         XAUTHSRC = $(LIBSRC)/Xau
          XLIBSRC = $(LIBSRC)/X
           XMUSRC = $(LIBSRC)/Xmu
       TOOLKITSRC = $(LIBSRC)/Xt
       AWIDGETSRC = $(LIBSRC)/Xaw
       OLDXLIBSRC = $(LIBSRC)/oldX
      XDMCPLIBSRC = $(LIBSRC)/Xdmcp
      BDFTOSNFSRC = $(FONTSRC)/bdftosnf
      BDFTOSNFSRC = $(FONTSRC)/clients/bdftosnf
      BDFTOPCFSRC = $(FONTSRC)/clients/bdftopcf
     MKFONTDIRSRC = $(FONTSRC)/clients/mkfontdir
         FSLIBSRC = $(FONTSRC)/lib/fs
    FONTSERVERSRC = $(FONTSRC)/server
     EXTENSIONSRC = $(TOP)/extensions
         XILIBSRC = $(EXTENSIONSRC)/lib/xinput
        PEXLIBSRC = $(EXTENSIONSRC)/lib/PEXlib
      PHIGSLIBSRC = $(EXTENSIONSRC)/lib/PEX

# $XConsortium: sunLib.tmpl,v 1.14.1.2 92/11/11 09:55:02 rws Exp $

SHLIBLDFLAGS = -assert pure-text
PICFLAGS = -pic

  DEPEXTENSIONLIB =
     EXTENSIONLIB = -lXext

          DEPXLIB = $(DEPEXTENSIONLIB)
             XLIB = $(EXTENSIONLIB) -lX11

        DEPXMULIB = $(USRLIBDIR)/libXmu.sa.$(SOXMUREV)
       XMULIBONLY = -lXmu
           XMULIB = -lXmu

       DEPOLDXLIB =
          OLDXLIB = -loldX

      DEPXTOOLLIB = $(USRLIBDIR)/libXt.sa.$(SOXTREV)
         XTOOLLIB = -lXt

        DEPXAWLIB = $(USRLIBDIR)/libXaw.sa.$(SOXAWREV)
           XAWLIB = -lXaw

        DEPXILIB =
           XILIB = -lXi

        DEPPEXLIB =
           PEXLIB = -lPEX5

        SOXLIBREV = 4.10
          SOXTREV = 4.10
         SOXAWREV = 5.0
        SOOLDXREV = 4.10
         SOXMUREV = 4.10
        SOXEXTREV = 4.10
      SOXINPUTREV = 4.10
         SOPEXREV = 1.0

      DEPXAUTHLIB = $(USRLIBDIR)/libXau.a
         XAUTHLIB =  -lXau
      DEPXDMCPLIB = $(USRLIBDIR)/libXdmcp.a
         XDMCPLIB =  -lXdmcp

        DEPPHIGSLIB = $(USRLIBDIR)/libphigs.a
           PHIGSLIB =  -lphigs

       DEPXBSDLIB = $(USRLIBDIR)/libXbsd.a
          XBSDLIB =  -lXbsd

 LINTEXTENSIONLIB = $(LINTLIBDIR)/llib-lXext.ln
         LINTXLIB = $(LINTLIBDIR)/llib-lX11.ln
          LINTXMU = $(LINTLIBDIR)/llib-lXmu.ln
        LINTXTOOL = $(LINTLIBDIR)/llib-lXt.ln
          LINTXAW = $(LINTLIBDIR)/llib-lXaw.ln
           LINTXI = $(LINTLIBDIR)/llib-lXi.ln
          LINTPEX = $(LINTLIBDIR)/llib-lPEX5.ln
        LINTPHIGS = $(LINTLIBDIR)/llib-lphigs.ln

          DEPLIBS = $(DEPXAWLIB) $(DEPXMULIB) $(DEPXTOOLLIB) $(DEPXLIB)

         DEPLIBS1 = $(DEPLIBS)
         DEPLIBS2 = $(DEPLIBS)
         DEPLIBS3 = $(DEPLIBS)

# -------------------------------------------------------------------------
# Imake rules for building libraries, programs, scripts, and data files
# rules:  $XConsortium: Imake.rules,v 1.123 91/09/16 20:12:16 rws Exp $

# -------------------------------------------------------------------------
# start of Imakefile

DESTDIR=${OZROOT}
IMAGEDIR=/lib/image-template

all::

install:: catalog.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) catalog.oi $(DESTDIR)$(IMAGEDIR)

install:: catalog.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) catalog.uc $(DESTDIR)$(IMAGEDIR)

install:: catalog.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) catalog.ur $(DESTDIR)$(IMAGEDIR)

install:: class.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) class.oi $(DESTDIR)$(IMAGEDIR)

install:: class.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) class.uc $(DESTDIR)$(IMAGEDIR)

install:: class.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) class.ur $(DESTDIR)$(IMAGEDIR)

install:: dns.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) dns.oi $(DESTDIR)$(IMAGEDIR)

install:: dns.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) dns.uc $(DESTDIR)$(IMAGEDIR)

install:: dns.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) dns.ur $(DESTDIR)$(IMAGEDIR)

install:: launcher.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) launcher.oi $(DESTDIR)$(IMAGEDIR)

install:: launcher.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) launcher.uc $(DESTDIR)$(IMAGEDIR)

install:: launcher.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) launcher.ur $(DESTDIR)$(IMAGEDIR)

install:: miniom.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) miniom.oi $(DESTDIR)$(IMAGEDIR)

install:: miniom.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) miniom.uc $(DESTDIR)$(IMAGEDIR)

install:: miniom.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) miniom.ur $(DESTDIR)$(IMAGEDIR)

install:: name.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) name.oi $(DESTDIR)$(IMAGEDIR)

install:: name.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) name.uc $(DESTDIR)$(IMAGEDIR)

install:: name.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) name.ur $(DESTDIR)$(IMAGEDIR)

install:: om.oi
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) om.oi $(DESTDIR)$(IMAGEDIR)

install:: om.uc
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) om.uc $(DESTDIR)$(IMAGEDIR)

install:: om.ur
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) om.ur $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.catalog
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.catalog $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.class
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.class $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.dns
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.dns $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.launcher
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.launcher $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.miniom
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.miniom $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.name
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.name $(DESTDIR)$(IMAGEDIR)

install:: preload-classes.om
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-classes.om $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.catalog
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.catalog $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.class
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.class $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.dns
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.dns $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.launcher
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.launcher $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.miniom
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.miniom $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.name
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.name $(DESTDIR)$(IMAGEDIR)

install:: preload-codes.om
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-codes.om $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.catalog
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.catalog $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.class
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.class $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.dns
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.dns $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.launcher
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.launcher $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.miniom
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.miniom $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.name
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.name $(DESTDIR)$(IMAGEDIR)

install:: preload-configurations.om
	@if [ -d $(DESTDIR)$(IMAGEDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(IMAGEDIR)); fi
	$(INSTALL) -c $(INSTDATFLAGS) preload-configurations.om $(DESTDIR)$(IMAGEDIR)

# -------------------------------------------------------------------------
# common rules for all Makefiles - do not edit

emptyrule::

clean::
	$(RM_CMD) "#"*

Makefile::
	-@if [ -f Makefile ]; then set -x; \
	$(RM) Makefile.bak; $(MV) Makefile Makefile.bak; \
	else exit 0; fi
	$(IMAKE_CMD) -DTOPDIR=$(TOP) -DCURDIR=$(CURRENT_DIR)

tags::
	$(TAGS) -w *.[ch]
	$(TAGS) -xw *.[ch] > TAGS

saber:
	# load $(ALLDEFINES) $(SRCS)

osaber:
	# load $(ALLDEFINES) $(OBJS)

# -------------------------------------------------------------------------
# empty rules for directories that do not have SUBDIRS - do not edit

install::
	@echo "install in $(CURRENT_DIR) done"

install.man::
	@echo "install.man in $(CURRENT_DIR) done"

Makefiles::

includes::

# -------------------------------------------------------------------------
# dependencies generated by makedepend

