# There is no need to preload any executable codes for a NameDirectory
# if the `Master Class' (a Class object which manages all release
# classes) is on the same executor.  If it is not, the NameDirectory
# needs following codes.

# NameDirectory
#  ResolverResponsibility
# Resolver
# String
# Dictionary<String,global ResolvableObject>
# Assoc<String,global ResolvableObject>
# Set<Assoc<String,global ResolvableObject>>
# ArrayOfCharOperators
