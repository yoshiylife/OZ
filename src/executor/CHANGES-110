Version 1.10
主な修正内容
	・起動オブション -G のデフォルトをモニタ表示ありに変更した。
	　（従って、-G を指定するとモニタ表示なしとなる）
	・現時点ではオブジェクト（セル）用のヒープの残りが少なくなると
	　自動的にＧＣを行うになっているが、その自動ＧＣの処理と OT の変化と
	　が重なると、SIGSEGV となる不具合を修正した。
	・標準エラー出力のモードを常に通常モードになるように修正した。
	　（従って、標準エラー出力はブロックモードとなる）
	・起動オブション -l を指定した時に kterm の起動が失敗（DISPLAYが不正等）
	　した場合にエグゼキュータを異常終了するように修正した。
	・OhMapHashTable2()で要素に適用した関数の戻り値が０以外となった
	　回数を返していたのを、OhMapHashTable()と同じく、０を返した回数を
	　返すように修正した。（この修正により、正しくプロセスをインスペクト
	　できるようになる）
